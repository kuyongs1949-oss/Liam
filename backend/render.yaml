services:
  - type: web
    name: noise-assessment-backend
    env: node
    buildCommand: npm ci --only=production
    startCommand: node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
